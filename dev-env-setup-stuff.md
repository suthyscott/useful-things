## If someone's bash is opening in the root of the computer rather than root user:
Right click on git bash and open the properties tab. Change the `Start in` section to have the file path to the root user

